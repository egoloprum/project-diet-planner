import { CreateCustomRecipeForm } from "@/src/features/(blog)/create-custom-recipe-form";

const page = ({}) => {
  return (
    <div>
      blah2
      <CreateCustomRecipeForm />
    </div>
  );
};

export default page;
